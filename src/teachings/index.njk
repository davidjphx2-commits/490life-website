---
layout: base.njk
title: "Teachings"
description: "Weekly teachings from Scripture — context, the original words, and what they ask of us."
permalink: /teachings/
tags: []
eleventyExcludeFromCollections: true
---

<section>
  <div class="container">
    <div class="center">
      <div class="eyebrow">The Weekly Table</div>
      <h2 class="section-title">Teachings.</h2>
      <p class="lead narrow">Scripture taken seriously &mdash; the context, the original words, and what it asks of us. Some weeks that&rsquo;s a single passage; some weeks it&rsquo;s several verses woven into one study. New teachings land every week; <a href="/#subscribe">join the list</a> and they come to you.</p>
    </div>
    <div class="teaching-list">
      {% for post in collections.teachings | reverse %}
      <article class="teaching-card">
        <div class="passage-badge">{{ post.data.passage }}</div>
        <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
        <div class="date">{{ post.date | readableDate }}</div>
        <p class="excerpt">{{ post.data.description }}</p>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

{% include "signup.njk" %}
